var assert = require('assert');
describe('Array', function() {
  describe('e2e', function() {
    it('should open browser and do something', function() {
      assert.equal([1, 2, 3, 4].indexOf(4), -1);
    });
  });
});
